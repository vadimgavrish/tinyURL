<title>TinyURL</title>

<% if (user) { %>

    <p>Hello <%= user.email %>!</p>

    <form method='post' action='/logout'>
        <button>Log out</button>
    </form>

<% } else { %>

    <form method='post' action='/login'>
        <input name='username'><button>Sign in</button>
    </form>

<% }; %>
