<!DOCTYPE html>
<html lang="en">
    <head>
        <title>TinyApp</title>
    </head>
    <body class='container'>
        <header>
            <% include ./partials/_header %>
        </header>

        <div>

            <h3>URL's</h3>
                <% for (key in urls) { %>

                    <li style="list-style-type: none"><%= key %> - <%= urls[key]%></li>

                <% }; %>
            
            </li>
        </div>
        
    </body>
</html>
